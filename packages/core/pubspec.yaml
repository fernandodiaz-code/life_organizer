name: life_core
description: Shared models and infrastructure services for Life Organizer.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.4

resolution: workspace

dependencies:
  http: ^1.2.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
